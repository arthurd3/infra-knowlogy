# O nome `web` no DNS do cluster — o outro nome que o Caddyfile do edge já usa.
apiVersion: v1
kind: Service
metadata:
  name: web
  labels: {app: web}
spec:
  selector: {app: web}
  ports:
    - {name: http, port: 8080, targetPort: http}
