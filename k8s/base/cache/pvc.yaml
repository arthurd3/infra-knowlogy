# ≙ o volume nomeado `redisdata` do Compose. Deployment não tem
# volumeClaimTemplate — o PVC é declarado à parte e referenciado.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redisdata
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests: {storage: 256Mi}
