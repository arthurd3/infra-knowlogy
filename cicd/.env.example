# Copiado para cicd/.env pelo cicd-up.sh. As portas ficam aqui para os três
# módulos poderem rodar ao mesmo tempo: 8080 é a stack, 8081 é o kind, 8090 é
# o Jenkins.
JENKINS_PORT=8090
REGISTRY_PORT=5001
